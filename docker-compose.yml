version: '3'

services:
   front-end:
     build:
        context: front-end
        dockerfile: Dockerfile
     hostname: front
     ports:
       - "3001:3000"
   back-end:
     build:
        context: back-end
        dockerfile: Dockerfile
     hostname: back
     ports:
       - "8080:8080"
   database:
     build:
        context: database
        dockerfile: Dockerfile
     hostname: database
     ports: 
       - "3306:3306"